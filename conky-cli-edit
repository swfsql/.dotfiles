
in the file diskio.c, near the:

/* TODO: move this correction from kB to kB/s elsewhere
 *   * (or get rid of it??) */
human_readable((val / update_interval) * 1024LL, p, p_max_size);


comment that out and add 

double valr = val / update_interval / 1024.0; 
if(val != 0 && valr < 0.1) spaced_print(p, p_max_size, "%.1f", 8, 0.1 );
else spaced_print(p, p_max_size, "%.1f", 8, valr);


